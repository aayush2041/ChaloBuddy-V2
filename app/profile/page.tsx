"use client";
import { useEffect,useState } from "react";
export default function Profile(){const [user,setUser]=useState<any>(null);const [error,setError]=useState("");useEffect(()=>{fetch("/api/profile").then(r=>r.json()).then(j=>{if(!j.success)throw new Error(j.error?.message);setUser(j.data)}).catch(e=>setError(e.message))},[]);if(error)return <div className="container-x py-16"><div className="card p-10 text-center">{error}</div></div>;if(!user)return <div className="container-x py-16"><div className="h-60 animate-pulse rounded-3xl bg-gray-100"/></div>;return <div className="container-x py-10 md:py-14"><div className="card p-8"><div className="flex flex-col gap-5 md:flex-row md:items-center"><div className="grid h-24 w-24 place-items-center rounded-full bg-mint text-4xl font-black tracking-tight md:text-5xl">{user.name[0]}</div><div><h1 className="text-4xl font-black tracking-tight md:text-5xl">{user.name}</h1><p className="mt-1 text-gray-500">{user.email}</p><p className="mt-3 text-gray-700">{user.profile?.bio||"Add a short bio to help travel companions get to know you."}</p></div></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><Stat label="Trips completed" value={user.profile?.tripsCompleted||0}/><Stat label="Cities visited" value={user.profile?.citiesVisited||0}/><Stat label="Companion rating" value={(user.profile?.companionRating||5).toFixed(1)}/></div></div></div>}
function Stat({label,value}:{label:string;value:string|number}){return <div className="rounded-2xl bg-sand p-5"><div className="text-2xl font-black">{value}</div><div className="mt-1 text-sm text-gray-500">{label}</div></div>}
