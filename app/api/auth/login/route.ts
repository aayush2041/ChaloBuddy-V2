import { prisma } from "@/lib/db"; import { createSession, verifyPassword } from "@/lib/auth"; import { fail, ok, json } from "@/lib/api"; import { loginSchema } from "@/lib/validation";
export async function POST(req: Request) { try { const parsed = loginSchema.safeParse(await json(req)); if (!parsed.success) return fail("VALIDATION_ERROR", "Enter a valid email and password"); const user = await prisma.user.findUnique({ where: { email: parsed.data.email.toLowerCase() } }); if (!user || !(await verifyPassword(parsed.data.password, user.passwordHash))) return fail("INVALID_CREDENTIALS", "Email or password is incorrect", 401); await createSession(user.id); return ok({ id: user.id, name: user.name, email: user.email }); } catch (e) { console.error(e); return fail("SERVER_ERROR", "Unable to sign in", 500); } }
