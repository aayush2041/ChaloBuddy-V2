"use client";
import Link from "next/link"; import { useRouter } from "next/navigation"; import { useState } from "react"; import { ArrowRight, LockKeyhole, Mail, UserRound } from "lucide-react";
export default function Signup(){const router=useRouter();const [form,setForm]=useState({name:"",email:"",password:"",confirmPassword:""});const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");try{const r=await fetch("/api/auth/signup",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(form)});const j=await r.json();if(!r.ok)throw new Error(j.error?.message||"Unable to create account");router.push("/explore")}catch(e:any){setError(e.message)}finally{setLoading(false)}}return <div className="container-x grid min-h-[calc(100vh-72px)] items-center py-12 lg:grid-cols-[.9fr_1fr] lg:gap-16"><div className="hidden lg:block"><span className="badge">Start traveling together</span><h1 className="mt-5 text-6xl font-black leading-[.95] tracking-[-.05em]">Good trips start with good people.</h1><p className="mt-5 max-w-lg text-lg leading-8 text-[#68756e]">Create your profile, discover journeys and find travelers who match the way you like to travel.</p></div><form onSubmit={submit} className="card mx-auto w-full max-w-md p-7 md:p-9"><div className="badge">Join ChaloBuddy</div><h2 className="mt-5 text-3xl font-black tracking-tight">Create your account</h2><p className="mt-2 text-sm text-[#68756e]">It only takes a minute to get started.</p>{error&&<div className="mt-5 rounded-2xl border border-red-200 bg-red-50 p-3 text-sm font-semibold text-red-700">{error}</div>}<Field icon={<UserRound size={17}/>} label="Name" placeholder="Your name" value={form.name} onChange={v=>setForm({...form,name:v})}/><Field icon={<Mail size={17}/>} label="Email" type="email" placeholder="you@example.com" value={form.email} onChange={v=>setForm({...form,email:v})}/><Field icon={<LockKeyhole size={17}/>} label="Password" type="password" placeholder="8+ characters" value={form.password} onChange={v=>setForm({...form,password:v})}/><Field icon={<LockKeyhole size={17}/>} label="Confirm password" type="password" placeholder="Repeat password" value={form.confirmPassword} onChange={v=>setForm({...form,confirmPassword:v})}/><button disabled={loading} className="mt-6 flex w-full items-center justify-center gap-2 rounded-2xl bg-[#1f7a52] py-3.5 font-black text-white transition hover:bg-[#155f3e] disabled:opacity-50">{loading?"Creating…":"Create account"}<ArrowRight size={17}/></button><p className="mt-6 text-center text-sm text-[#68756e]">Already have an account? <Link className="font-black text-[#1f7a52]" href="/login">Log in</Link></p></form></div>}
function Field({icon,label,value,onChange,placeholder,type="text"}:{icon:React.ReactNode;label:string;value:string;onChange:(v:string)=>void;placeholder:string;type?:string}){return <label className="mt-4 block text-sm font-bold">{label}<div className="relative mt-2"><span className="absolute left-3 top-1/2 -translate-y-1/2 text-[#8a948f]">{icon}</span><input required type={type} minLength={type==="password"?8:undefined} placeholder={placeholder} value={value} onChange={e=>onChange(e.target.value)} className="w-full rounded-2xl border py-3.5 pl-10 pr-4"/></div></label>}
