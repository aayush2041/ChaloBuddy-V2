"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {TripCard} from "@/components/trip-card";
export default function MyTrips(){const [items,setItems]=useState<any[]>([]);const [error,setError]=useState("");useEffect(()=>{fetch("/api/trips").then(r=>r.json()).then(j=>setItems(j.data||[])).catch(()=>setError("Unable to load trips"))},[]);return <div className="container-x py-10 md:py-14"><p className="badge">My Trips</p><h1 className="mt-3 text-4xl font-black tracking-tight md:text-5xl">Your travel dashboard.</h1><p className="mt-3 max-w-2xl text-[#68756e]">Create and manage trips from one place.</p><div className="mt-7">{error?<div className="card p-8">{error}</div>:items.length?<div className="grid gap-5 md:grid-cols-2">{items.map(t=><TripCard key={t.id} trip={t}/>)}</div>:<div className="card p-10 text-center"><h2 className="text-xl font-black">No published trips yet</h2><Link href="/list-trip" className="mt-4 inline-block rounded-full bg-leaf px-5 py-2 font-bold text-white">List a Trip</Link></div>}</div></div>}
