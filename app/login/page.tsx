"use client";
import Link from "next/link"; import { useRouter } from "next/navigation"; import { useState } from "react"; import { ArrowRight, LockKeyhole, Mail } from "lucide-react";
export default function Login(){const router=useRouter();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});const j=await r.json();if(!r.ok)throw new Error(j.error?.message||"Unable to sign in");router.push("/explore")}catch(e:any){setError(e.message)}finally{setLoading(false)}}return <div className="container-x grid min-h-[calc(100vh-72px)] items-center py-12 lg:grid-cols-[.9fr_1fr] lg:gap-16"><div className="hidden lg:block"><span className="badge">Welcome back</span><h1 className="mt-5 text-6xl font-black leading-[.95] tracking-[-.05em]">Your next trip is closer than you think.</h1><p className="mt-5 max-w-lg text-lg leading-8 text-[#68756e]">Sign in to manage trips, requests, saved journeys and your travel profile.</p></div><form onSubmit={submit} className="card mx-auto w-full max-w-md p-7 md:p-9"><div className="badge">ChaloBuddy account</div><h2 className="mt-5 text-3xl font-black tracking-tight">Log in</h2><p className="mt-2 text-sm text-[#68756e]">Continue planning with your travel community.</p>{error&&<div className="mt-5 rounded-2xl border border-red-200 bg-red-50 p-3 text-sm font-semibold text-red-700">{error}</div>}<label className="mt-7 block text-sm font-bold">Email<div className="relative mt-2"><Mail size={17} className="absolute left-3 top-1/2 -translate-y-1/2 text-[#8a948f]"/><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="w-full rounded-2xl border py-3.5 pl-10 pr-4" placeholder="you@example.com"/></div></label><label className="mt-4 block text-sm font-bold">Password<div className="relative mt-2"><LockKeyhole size={17} className="absolute left-3 top-1/2 -translate-y-1/2 text-[#8a948f]"/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="w-full rounded-2xl border py-3.5 pl-10 pr-4" placeholder="Your password"/></div></label><button disabled={loading} className="mt-6 flex w-full items-center justify-center gap-2 rounded-2xl bg-[#10231b] py-3.5 font-black text-white transition hover:bg-[#1f7a52] disabled:opacity-50">{loading?"Signing in…":"Log in"}<ArrowRight size={17}/></button><p className="mt-6 text-center text-sm text-[#68756e]">New here? <Link className="font-black text-[#1f7a52]" href="/signup">Create an account</Link></p></form></div>}
