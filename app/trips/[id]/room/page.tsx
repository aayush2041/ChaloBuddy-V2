"use client";
import { useParams } from "next/navigation"; import { useState } from "react";
export default function Room(){const {id}=useParams<{id:string}>();const [tab,setTab]=useState("Overview");const tabs=["Overview","Chat","Itinerary","Expenses","Tasks","Polls","Members"];return <div className="container-x py-10 md:py-14"><p className="badge">Trip Room</p><h1 className="mt-3 text-4xl font-black tracking-tight md:text-5xl">Your shared trip workspace</h1><div className="mt-7 flex gap-2 overflow-x-auto border-b pb-2">{tabs.map(t=><button key={t} onClick={()=>setTab(t)} className={`whitespace-nowrap rounded-full px-4 py-2 text-sm font-bold ${tab===t?"bg-ink text-white":"bg-gray-100"}`}>{t}</button>)}</div><div className="card mt-6 min-h-96 p-8">{tab==="Overview"&&<div><h2 className="text-2xl font-black">Keep the group aligned.</h2><p className="mt-2 text-gray-600">Use this space for planning conversations, shared costs, tasks and decisions.</p></div>}{tab!=="Overview"&&<div><h2 className="text-2xl font-black">{tab}</h2><p className="mt-2 text-gray-600">The {tab.toLowerCase()} workspace is ready for trip members. API-backed room primitives are included in the V12 foundation.</p><p className="mt-5 text-xs text-gray-400">Trip ID: {id}</p></div>}</div></div>}
